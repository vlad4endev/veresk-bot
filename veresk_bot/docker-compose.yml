services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      DATABASE_PATH: /app/data/veresk.db
      SESSIONS_DIR: /app/data/sessions
    expose:
      - "3005"
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
      - ./certs:/app/certs:ro
    depends_on:
      - redis

  # MAX-бот (анкета как в Telegram, без Mini App).
  # Токен: Настройки → MAX в админке (runtime_settings) или MAX_BOT_TOKEN в .env.
  # Без токена процесс ждёт, не падает в crash-loop.
  max_bot:
    build: .
    command: ["python", "-m", "max_bot.main"]
    restart: unless-stopped
    env_file: .env
    environment:
      DATABASE_PATH: /app/data/veresk.db
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
      - ./certs:/app/certs:ro

  nginx:
    image: nginx:alpine
    restart: unless-stopped
    ports:
      # Внутренний HTTP — проксируйте сюда с хоста (admin.veresk-flowers.ru → :3005)
      - "3005:3005"
      # SSL внутри Docker: раскомментируйте и используйте nginx.ssl.conf.example
      # - "80:80"
      # - "443:443"
    volumes:
      - ./miniapp:/usr/share/nginx/html/miniapp:ro
      - ./adminapp:/usr/share/nginx/html/admin:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      # - ./ssl:/etc/nginx/ssl:ro
    depends_on:
      - bot

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    volumes:
      - redis_data:/data

volumes:
  redis_data:
